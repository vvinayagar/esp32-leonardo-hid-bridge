name: esp32_hid_bridge
publish_to: 'none'
description: Flutter app for ESP32-Leonardo HID bridge
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.15.4
  permission_handler: ^10.4.3
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
